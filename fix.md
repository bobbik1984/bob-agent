# 待修复清单 (UI & Bugs)

- [x] **P2P 二维码 Relay 地址**：其实已经是 relay.bobbik.org，无需修改。
- [x] **PC 端连接设置卡片拥挤**：已将卡片网格限制为最多2列。
- [x] **手机端模型中心排版超出屏幕**：“主力模型”和“牛马模型”卡片已在手机端改为单列。
- [x] **本地模型下载无响应 / HF 默认源**：已强制在 Android 端改用 `rustls-tls`，修复了 Android 原生 HTTPS 证书加载导致的静默失败问题。
- [x] **手机端配置 API 后无网络请求响应**：同上，由于更换了 TLS 引擎，API 请求现已恢复正常。

- [x] **PC 端窄屏 Onboarding 误判修复**：重写 SetupWizard 逻辑，改用 userAgent 检测手机，确保 PC 用户缩小窗口时依然展示微信绑定。
- [x] **设置页连接卡片样式统一**：在 SettingsConnections 移除多余的 Hover 交互与边框，强制等高网格，使所有配置卡片高度对齐。
- [x] **知识图谱窄屏容器重叠修复**：在 KnowledgeGraphView 增加自适应响应式切换，检测到窄屏时改用纵向布局。
- [x] **日程视图顶部标签栏响应式优化**：在 InboxView 移除窄屏下的左右 padding 并设置等分比例，使得移动端 Tabs 满宽贴边展示。
- [x] **手机端顶部状态栏对比度修复**：在 App.vue 为移动端顶部安全区加装纯黑背景遮罩，确保在浅色主题下依然能看清手机系统的白色状态栏文字。
